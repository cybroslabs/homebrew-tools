## ouroctl apply business-objects

Apply business objects from JSON file

### Synopsis

Apply business objects from a JSON file to create or update.

```
ouroctl apply business-objects [flags]
```

### Examples

```
  # Apply business objects from a json file
  ouroctl apply business-objects --file business-objects.json
  # Apply business objects from a json file and replace existing ones
  ouroctl apply business-objects --file business-objects.json --replace
```

### Options

```
  -f, --file string   Path to the template file to apply
  -h, --help          help for business-objects
  -r, --replace       Replace existing items (default: create new only)
```

### SEE ALSO

* [ouroctl apply](ouroctl_apply.md)	 - Apply templates and configurations to the system

###### Auto generated by spf13/cobra on 7-Jul-2026
