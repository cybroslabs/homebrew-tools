## ouroctl apply field-descriptors

Apply field descriptors to the system

### Synopsis

Applies field descriptors to the system based on the provided input.

```
ouroctl apply field-descriptors [flags]
```

### Examples

```
  # Apply field descriptors from a json file
  ouroctl apply field-descriptors --file field-descriptors.json
  # Apply field descriptors from a json file and replace existing ones
  ouroctl apply field-descriptors --file field-descriptors.json --replace
```

### Options

```
  -f, --file string   Path to the template file to apply
  -h, --help          help for field-descriptors
  -r, --replace       Enable replace mode If not set the application will only create new objects and will not touch existing ones.
```

### SEE ALSO

* [ouroctl apply](ouroctl_apply.md)	 - Apply changes to the system

###### Auto generated by spf13/cobra on 1-Dec-2025
