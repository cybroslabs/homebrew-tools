## ouroctl apply roles

Apply role configurations from JSON file

### Synopsis

Apply role configurations from a JSON file to update role definitions in the system.

```
ouroctl apply roles [flags]
```

### Examples

```
  # Apply role configs from a json file
  ouroctl apply roles --file roles.json

  # Create or replace existing objects
  ouroctl apply roles --file roles.json --replace
```

### Options

```
  -f, --file string   Path to the roles file to apply
  -h, --help          help for roles
  -r, --replace       Replace existing objects (default: create new only)
```

### SEE ALSO

* [ouroctl apply](ouroctl_apply.md)	 - Apply templates and configurations to the system

###### Auto generated by spf13/cobra on 11-Jun-2026
